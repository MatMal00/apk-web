import { FC } from "react";

export const Login: FC = () => {
    return (
        <section>
            <div>
                <h1 className="font-bold uppercase">apk web</h1>
            </div>
        </section>
    );
};
